#Write a program that asks the user for a string and prints the first and last characters of the string.

userInput = input('Enter a string: ')
first = userInput[0]
last = userInput[-1]
print(f"first letter is {first} and the last letter is {last}")
